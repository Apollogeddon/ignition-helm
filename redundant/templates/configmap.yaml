{{- if .Values.ignition.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ignition.name" . }}-config
  labels:
    {{- include "ignition.labels" . | nindent 4 }}
data: {{ .Values.ignition.config | toYaml | nindent 2 }}
{{- end }}