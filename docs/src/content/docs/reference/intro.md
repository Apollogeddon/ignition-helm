---
title: Reference Documentation
description: API Reference for the charts.
---

This section is auto-generated using `helm-docs`.

To populate the configuration tables for `failover`, `scaleout`, and `common`, please run:

```bash
helm-docs
```

Or via npm:

```bash
cd docs
npm run gen-docs
```
